Add GeoDatabase source, .gdb dataset button, filters, and attribute grid.

- Add GDB_SOURCE with isLocal flag so loaded .gdb datasets can be filtered
  by query, state, and type without making an API call.
- Add "GeoDatabase" data set and "Select .gdb Dataset" button that opens
  the file picker.
- Store original .gdb attributes on each GeoRecord so the bottom grid can
  show all fields.
- Add ResultsGrid component with a horizontally scrollable table that
  appears at the bottom of the Filters tab; clicking a row zooms to that
  record.
- Skip non-geocoded records in GlobeMap instead of placing them at 0,0.
- Add data/sample.geojson for quick manual testing.
- Update AGENTS.md with the new workflow.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
